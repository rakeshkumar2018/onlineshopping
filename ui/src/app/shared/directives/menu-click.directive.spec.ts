import { MenuClickDirective } from './menu-click.directive';

describe('MenuClickDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuClickDirective();
    expect(directive).toBeTruthy();
  });
});
